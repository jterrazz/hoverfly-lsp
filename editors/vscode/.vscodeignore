# Sources and build inputs (the published .vsix ships only the bundled output).
src/**
test/**
tsconfig.json
esbuild.config.mjs

# Build, test and lint artefacts (the tsc buildinfo, and the .vsix itself).
.artifacts/**

# Source maps (large; not needed at runtime).
**/*.map

# Tooling / VCS noise.
.gitignore
node_modules/**
